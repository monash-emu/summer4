# Step 23 landing record

Step 23 is section *Step 23* of `plans/rate-dispatch-and-defer.plan.md`.
That plan was committed with step 22, so it is not part of this branch's diff
against `feat/rate-array-dispatch`. This file is the plan the branch check
requires for the `Defer` changes.

The implementation, tests, and `examples/notebooks/16-deferred-functions.ipynb`
are on `feat/rate-defer`. The roadmap marks step 23 `done`.
